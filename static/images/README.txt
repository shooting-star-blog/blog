IMPORTANT: Do NOT place cover images here.

Cover images must go in assets/images/ (not static/images/) so Hugo's
image pipeline can process them to WebP and generate responsive srcset.

Images placed in static/images/ are served as-is and bypass the pipeline.
